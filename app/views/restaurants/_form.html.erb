<%= simple_form_for(restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category %>
  <% if @exists %>
  <%= f.submit ("Update") %>
  <% else %>
  <%= f.submit ("Create") %>
  <% end %>
<% end %>
